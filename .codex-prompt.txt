You will rewrite ONLY these files to give the React UI a DEEIX-Chat-style facelift. The app is Vite + React 19 + Tauri 2. NO new dependencies. Keep all existing props, state, Tauri api calls, and Chinese labels EXACTLY as-is. Only change JSX structure (class names) and CSS.

=== DESIGN SYSTEM (DEEIX-Chat warm dark theme) ===

CSS custom properties (put in :root in App.css):
  --bg: #2e2c28;                /* app background, warm dark */
  --panel: #38352f;             /* sidebar / cards */
  --panel-2: #423e37;           /* hover, headers */
  --panel-3: #4d493f;           /* selected / elevated */
  --border: #4a463e;            /* subtle border */
  --border-strong: #5a5448;
  --text: #ece6d8;              /* warm off-white */
  --text-dim: #a39d8e;           /* muted */
  --text-faint: #7a7569;
  --primary: #c8643a;           /* terracotta */
  --primary-hover: #d97a4e;
  --primary-soft: rgba(200,100,58,0.14);
  --ok: #84cc7a;
  --fail: #e57373;
  --radius: 10px;
  --radius-sm: 6px;
  --radius-lg: 14px;
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.18), 0 1px 1px rgba(0,0,0,0.12);
  --shadow-md: 0 4px 10px rgba(0,0,0,0.22), 0 2px 4px rgba(0,0,0,0.16);
  --shadow-lg: 0 12px 30px rgba(0,0,0,0.35), 0 6px 12px rgba(0,0,0,0.22);
  --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', system-ui, sans-serif;
  --font-serif: 'Iowan Old Style', 'Palatino Linotype', Palatino, 'Songti SC', 'STSong', 'Source Han Serif SC', Georgia, serif;
  --font-mono: ui-monospace, 'Cascadia Code', 'JetBrains Mono', Consolas, monospace;

Base body: font-family var(--font-sans), font-size 13.5px, antialiased.

=== FILE 1: E:\develop\flow\src\App.css (FULL REWRITE) ===

Layout:
- .app: column flex, full viewport
- .topbar: 52px tall, flex row, padding 0 20px, background var(--panel), border-bottom var(--border). Inside:
    .brand uses font-family var(--font-serif), font-size 19px, font-weight 500, letter-spacing -0.01em, color var(--text). Render a small terracotta dot before brand: ::before content '', 8px circle, background var(--primary), margin-right 10px, box-shadow 0 0 12px rgba(200,100,58,0.5).
    .brand-sub: color var(--text-dim), font-size 12px, margin-left 12px, font-weight 400.
    .err: ml auto, background rgba(229,115,115,0.12), border 1px solid rgba(229,115,115,0.3), color var(--fail), padding 5px 12px, border-radius 999px, font-size 12px, cursor pointer, max-width 50%, truncated.
- .cols: grid-template-columns 280px 1fr 320px, gap 0, flex 1, min-height 0.

Panes:
- .pane: flex col, min-height 0, background var(--panel), border-right 1px solid var(--border).
- .pane-center: background var(--bg), border-right 1px solid var(--border).
- .pane-right: border-right none.
- .pane-header: padding 14px 18px, font-weight 500, font-size 12px, text-transform uppercase, letter-spacing 0.08em, color var(--text-dim), display flex, align-items center, justify-content space-between, border-bottom 1px solid var(--border). NOT a different background — same as pane.
- .pane-body: flex 1, overflow-y auto, scrollbar-width thin, scrollbar-color var(--border-strong) transparent. Custom webkit scrollbar 8px wide, thumb var(--border-strong) rounded, track transparent.
- .empty: padding 32px 20px, color var(--text-faint), text-align center, font-size 13px.

Task rows (chat-list style):
- .task-row: margin 4px 8px, padding 10px 12px, border-radius var(--radius-sm), cursor pointer, transition background 120ms ease.
- .task-row:hover: background var(--panel-2).
- .task-row.selected: background var(--panel-3). NO left border accent — use background only.
- .task-intent: font-size 13.5px, line-height 1.45, margin-bottom 6px, color var(--text), font-weight 450.
- .task-meta: display flex, gap 5px, flex-wrap wrap.

Badges (pill style):
- .badge: display inline-flex, align-items center, padding 1px 8px, border-radius 999px, font-size 10.5px, font-weight 500, background rgba(255,255,255,0.04), border 1px solid var(--border), color var(--text-dim), text-transform lowercase, letter-spacing 0.02em.
- .badge-dev: color #93c5fd, background rgba(147,197,253,0.08), border-color rgba(147,197,253,0.25).
- .badge-visual: color #fbbf24, background rgba(251,191,36,0.08), border-color rgba(251,191,36,0.25).
- .badge.state: color var(--primary), background var(--primary-soft), border-color rgba(200,100,58,0.3).

Buttons:
- .btn-mini: background transparent, border 1px solid var(--border), color var(--text), padding 5px 12px, border-radius 999px, cursor pointer, font-size 12px, font-weight 500, transition all 120ms.
- .btn-mini:hover: background var(--panel-2), border-color var(--border-strong).
- .btn-primary: same as btn-mini but background var(--primary), border-color var(--primary), color white. Hover: background var(--primary-hover), border-color var(--primary-hover).

Turn card (chat message):
- .turn-card: background var(--panel), border 1px solid var(--border), border-radius var(--radius), margin 16px 20px, overflow hidden, box-shadow var(--shadow-sm).
- .turn-head: padding 10px 16px, background transparent, border-bottom 1px solid var(--border), display flex, gap 10px, align-items center.
- .turn-body: padding 14px 16px, margin 0, white-space pre-wrap, font-family var(--font-mono), font-size 12.5px, line-height 1.6, color var(--text), background transparent.

Chat heading area:
- .chat-header: padding 16px 24px, border-bottom 1px solid var(--border).
- .chat-header-title: font-family var(--font-serif), font-size 17px, font-weight 500, color var(--text), line-height 1.4, letter-spacing -0.005em.
- .chat-header-sub: font-size 11px, color var(--text-faint), margin-top 4px, font-family var(--font-mono).

Empty hero:
- .hero: display flex, flex-direction column, align-items center, justify-content center, height 100%, padding 40px, gap 14px.
- .hero h1: font-family var(--font-serif), font-size 34px, font-weight 500, margin 0, letter-spacing -0.01em, color var(--text).
- .hero p: color var(--text-dim), font-size 14px, margin 0, max-width 420px, text-align center, line-height 1.6.

dim utility: .dim { color: var(--text-dim); font-size: 11.5px; }
dim-faint: .dim-faint { color: var(--text-faint); font-size: 11px; font-family: var(--font-mono); }

Probe row:
- .probe-row: display flex, gap 10px, align-items center, padding 6px 0, font-size 12.5px.
- .dot: width 7px, height 7px, border-radius 999px, flex-shrink 0.
- .dot.ok: background var(--ok), box-shadow 0 0 8px rgba(132,204,122,0.55).
- .dot.fail: background var(--fail), box-shadow 0 0 8px rgba(229,115,115,0.45).
- .agent-name: font-weight 500, min-width 56px, color var(--text).

File tree:
- .file-tree: list-style none, padding 4px 16px 12px, margin 0, font-family var(--font-mono), font-size 11.5px.
- .file-tree li: padding 3px 0, color var(--text-dim).

Modal:
- .modal-backdrop: position fixed, inset 0, background rgba(20,18,15,0.55), backdrop-filter blur(8px), display flex, align-items center, justify-content center, z-index 100, animation fadeIn 140ms ease.
- .modal: background var(--panel), border 1px solid var(--border), border-radius var(--radius-lg), padding 24px 26px, width 520px, max-width 92vw, box-shadow var(--shadow-lg). animation modalIn 180ms cubic-bezier(0.2,0.8,0.2,1).
- .modal h3: font-family var(--font-serif), font-size 20px, font-weight 500, margin 0 0 18px; letter-spacing -0.01em.
- .modal label: display block, font-size 11px, color var(--text-dim), margin-bottom 6px, text-transform uppercase, letter-spacing 0.06em, font-weight 500.
- .modal textarea, .modal select: width 100%, background var(--bg), color var(--text), border 1px solid var(--border), border-radius var(--radius-sm), padding 10px 12px, font-family inherit, font-size 13.5px, transition border-color 120ms, outline none.
- .modal textarea:focus, .modal select:focus: border-color var(--primary), box-shadow 0 0 0 3px var(--primary-soft).
- .modal textarea: resize vertical, min-height 100px, line-height 1.55.
- .modal .row: display flex, gap 14px.
- .modal .row > div: flex 1.
- .modal-actions: display flex, gap 10px, justify-content flex-end, margin-top 20px.
- .modal button: background transparent, color var(--text), border 1px solid var(--border), padding 8px 18px, border-radius var(--radius-sm), cursor pointer, font-size 13px, font-weight 500, transition all 120ms.
- .modal button:hover: background var(--panel-2), border-color var(--border-strong).
- .modal button.primary: background var(--primary), border-color var(--primary), color white.
- .modal button.primary:hover: background var(--primary-hover), border-color var(--primary-hover).
- .modal button.primary:disabled: opacity 0.5, cursor not-allowed, background var(--primary).

Workspace path display:
- .workspace-path: padding 8px 16px, font-family var(--font-mono), font-size 10.5px, color var(--text-faint), word-break break-all, border-top 1px dashed var(--border), border-bottom 1px dashed var(--border), margin 4px 0.

Refresh button: .refresh-btn extends btn-mini styling but width:100% and margin-top:10px.

Agents/workspace section padding wrapper: a .section class with padding 8px 16px 16px when needed.

Keyframes:
@keyframes fadeIn { from {opacity: 0} to {opacity: 1} }
@keyframes modalIn { from { opacity: 0; transform: translateY(8px) scale(0.98) } to { opacity: 1; transform: none } }

Reset body / html / #root: margin 0, padding 0, height 100%, overflow hidden, background var(--bg), color var(--text), font-family var(--font-sans), font-size 13.5px, -webkit-font-smoothing antialiased, -moz-osx-font-smoothing grayscale.
* { box-sizing: border-box; }

=== FILE 2: E:\develop\flow\src\App.tsx (KEEP everything; just keep existing classes) ===

Do NOT change hooks/handlers. Keep the file as-is structurally. The CSS handles the ::before dot via .brand selector. Do not touch.

=== FILE 3: E:\develop\flow\src\components\TaskList.tsx ===

Keep all props/handlers unchanged. Update structure:
- Change the new-task button className from 'btn-mini' to 'btn-primary'. Keep text '＋ 新建'.
- Empty state: <div className='empty'>还没有任务<br/><span className='dim-faint'>点击右上角 ＋ 新建 创建第一个</span></div>
- task-row markup otherwise unchanged.

=== FILE 4: E:\develop\flow\src\components\ThreadView.tsx ===

Keep prop signature.
- When no task: render
    <div className='pane pane-center'>
      <div className='pane-body'>
        <div className='hero'>
          <h1>Flow</h1>
          <p>在左侧新建任务，让 Claude 与 Codex 一起把它跑完。</p>
        </div>
      </div>
    </div>
- When task selected: replace pane-header with:
    <div className='chat-header'>
      <div className='chat-header-title'>{task.intent}</div>
      <div className='chat-header-sub'>{task.id} · {task.profile} · {task.mode}</div>
    </div>
- pane-body keeps the turn-card with badge 'created' and dim span containing task.created_at.
- The empty 'FSM 尚未启动 — Phase 2 接入后将在这里看到 Claude/Codex 轮次。' stays inside className='empty', with inline style marginTop: 24.

=== FILE 5: E:\develop\flow\src\components\WorkspacePane.tsx ===

Keep all props and tauri api calls (api.listWorkspaceFiles) unchanged.
- Two pane-header blocks: 'Agents' and 'Workspace'.
- Agents block contents: wrap in <div className='section'>. Render probe rows as before. Then <button className='refresh-btn' onClick={onRefreshProbes}>重新探活</button>.
- Workspace block: if no task render <div className='empty'>未选择任务</div>. If task and files.length===0 render <div className='workspace-path'>{task.workspace_path}</div> then <div className='empty'>workspace 为空</div>. If task and files.length>0 render <div className='workspace-path'>{task.workspace_path}</div> then the <ul className='file-tree'>.

=== FILE 6: E:\develop\flow\src\components\NewTaskDialog.tsx ===

Keep all props, state, and onSubmit signature unchanged.
- Replace the action row (the one with marginTop 16 and justifyContent flex-end) with <div className='modal-actions'>...buttons...</div>. Keep both buttons and their handlers.
- Keep Chinese option texts and labels.

=== HARD RULES ===
1. No new npm dependencies. Pure CSS only.
2. Do NOT remove any onClick handlers, prop drilling, or api.* calls.
3. Do NOT touch tauri-api.ts, main.tsx, or vite.config.ts.
4. After writing, run from E:\develop\flow:  node_modules\.bin\tsc.cmd --noEmit
   Report the exit code.
5. Keep file imports the same (no new component files).

Do all the writes, then run the type check, then briefly report what changed and the tsc exit code.
